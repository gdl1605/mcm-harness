# Contributing / 贡献指南

欢迎通过 Issue 或 Pull Request 改进 `mcm-harness`。安全漏洞或敏感信息泄露请按 [SECURITY.md](SECURITY.md) 私下报告。

## 修改范围

- 工作流与角色协议：`AGENTS.md`、`Workflow/`、`prompts/`、`templates/`。
- 机械辅助工具及测试：`scripts/`、`tests/`。
- 内置建模 Skill：`.agents/skills/mcm/`，保留它自身的版权与分发声明。
- 使用说明、许可说明和发布文档。

一次改动尽量解决一个明确问题。先阅读 [AGENTS.md](AGENTS.md) 和受影响模块文档，再同步修改角色提示词、模板、配置及必要测试；不要顺手重构无关模块。

## 不应改变的边界

- Leader 保持共享产物的唯一所有权，独立审查保持上下文隔离。
- 人工模型选择和最终人工交付不能由 Agent 模拟或自动跳过。
- JSON 与检查脚本只处理配置、路径、哈希、版本和状态，不以字段命中或分数代替语义判断。
- 新建模、论文经验需说明证据、适用条件与失效边界；不承诺获奖，不把个例提升为通用规则。
- 外部技能、模型或平台能力缺失时明确报告，不静默替换已要求的能力。

## 许可与数据

提交内容须为你有权贡献的原创内容，或具有兼容授权且保留了来源、版权和许可。除另有明确约定外，贡献按本项目 MIT License 提供；已有第三方材料仍适用其原许可。

不要提交凭据、私钥、个人绝对路径、真实运行目录、原始赛题附件、完整论文或私人数据。复现问题优先使用脱敏的最小示例或明确标注的合成数据；不要上传整个 run。外部依赖新增或许可变化时同步更新 [NOTICE.md](NOTICE.md) 和相关锁文件。

## 验证与 PR 说明

在仓库根目录运行：

```bash
python3 -m unittest discover -s tests -v
git diff --check
```

文档修改至少核对相对链接、命令和许可边界；代码或协议修改还应给出最小复现、预期产物和回归结果。测试通过不等于模型、论文或版权审查通过。

PR 请简要说明：

- 原问题、修改范围和受影响阶段。
- 文档、配置、提示词、模板之间如何保持一致。
- 运行过的检查及未完成的验证。
- 是否引入新依赖、数据、兼容性变化或人工决策边界变化。

发布前另执行 [发布清单](docs/releasing.md)，并在 [CHANGELOG.md](CHANGELOG.md) 的 `Unreleased` 下记录面向使用者的变化。
