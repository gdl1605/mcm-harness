# Bootstrap：默认初始化入口

用户 clone 仓库，在 Agent 中打开仓库后说“初始化”“执行初始化”“进行 init”或“bootstrap”，Agent 默认执行本流程。收到所需材料、完成准备后自动进入材料阅读与模型路线竞标，到 H1 等待用户选择模型；无需额外的“开始”prompt。无需让用户先选目录、拼命令或阅读全部阶段文档。

## Agent 执行顺序

1. 确认用户是在要求初始化当前 Harness。开发/修改初始化代码、解释 init、明确禁止执行、`git init` 等请求不触发 Bootstrap。仅 clone 或打开仓库也不自动执行命令。
2. 在仓库根目录使用 Python 3.10+ 运行：

   ```bash
   python3 scripts/bootstrap.py --prepare-only --json
   ```

   平台只有 `python` 或 `py -3` 时，可先核对版本后使用等价命令。Python 未安装时说明阻塞，不自动改系统环境。
3. 用户已经明确提供题目/附件路径、标题或运行目录时，转换为重复的 `--source`、`--title`、`--run-dir` 参数；带空格的路径必须正确引用。用户给材料目录时，只查看其授权目录并列出拟登记文件，不扫描其他位置。上传文件先取得真实本地路径；下载链接先通过可用工具保存到 `raw-sources/` 中未占用的文件名，再登记本地文件。脚本的 `--source` 不接受 URL；无法访问链接、需要登录或材料不可读时说明缺项，请用户上传或提供可访问位置，不绕过权限。
4. 按返回状态处理，不把退出码 0 等同于“全流程环境已就绪”：

   | 状态 | Agent 下一步 |
   | --- | --- |
   | `AWAITING_SOURCES` | 主动向用户提出下方材料问题并等待；不只是显示“目录创建成功”。收到路径/文件后，自动接着执行本次 Bootstrap，无需用户重复发 init。 |
   | `AWAITING_SOURCE_REVIEW` | 读取候选清单并按下方规则核对题目与全部附件；缺哪一类就只问哪一类。未齐备不建立 run。 |
   | `INITIALIZED` | 基础工作区已建立，自检通过；确认材料和当前阶段能力后，默认由 Leader 自动推进材料阅读、路线竞标与文献校准，到 H1 停止。脚本成功不替代材料完整性判断。 |
   | `EXISTING_RUN` | 保留来源、阶段及历史。先核对当前阶段和活动任务，必要时补充材料信息；仅衔接未完成的前半程，不重跑、不重复派工。已在 H1/最终人工交接时维持等待，不用 init 授权恢复后半程。 |
   | `BLOCKED` / 非零退出码 | 报告具体缺项或冲突；保留文件。来源变更、旧快照漂移、非空无效目录均不得用删除、覆盖或修改哈希“修复”。 |

5. 有 `skipped_source_entries` 时明确报告，不把未登记材料当作已可用。默认只登记 `raw-sources/` 顶层支持的文件，忽略隐藏文件和 Office 锁文件；不递归搜索、不跟随自动发现的符号链接、不解压压缩包。登记不等于已读懂附件，W0 仍须确认来源完整性和可读性。
6. 根据工具实际可用情况核对 subagent、文件/命令权限、文件读取与检索能力。后续绘图的两个本地 Skill 检查只验证入口文件哈希，不证明完整安装；`visualize-data` 和 `gpt-5.6-sol + high` 仍须由平台核实。不得自动下载技能、安装所有建模库、修改全局配置或声称完整环境已就绪。
7. 普通“初始化 / init”默认包含准备后的前半程推进。用户提供缺少的文件地址后，完成材料核对和自检就按 `AGENTS.md` 第 2 节实际调度材料阅读、交叉审查、宽候选竞标、文献校准、候选重构及 L2C 展示，到 H1 等待真人选择；不要再要求用户发送“开始解题”。此处不训练、求解、调参、正式清洗或写论文。先完成 W1–W4 和 L1 题意基线再开始 W5A，不因“自动竞标”跳过审题。仅缺后续正式绘图能力时报告限制，不无故阻塞可执行的前半程。

## 自动衔接与明确停止

- 材料齐备后可说：“好的，现在开始材料阅读与模型路线竞标；完成后我会展示候选方案，等你选择模型。”这只是进度说明，必须随后在同一轮实际推进，不能当作最终回复后结束。
- 自动衔接仅适用于用户正在初始化或补充所需材料的任务；上传文件供其他用途、讨论代码或给出示例地址不触发。
- 用户明确“只初始化，暂不开始阅读/竞标”时，预检和正式初始化都保留 `--setup-only`；它使返回的下一步为 `STOP_AFTER_SETUP`。收到后续路径仍沿用该限制，不能把补材料当作撤回限制。
- `--prepare-only` 是冻结前的技术预检，不表示用户要求准备后停止。确认材料后去掉此参数继续初始化；不要丢掉用户要求的 `--setup-only`。
- 脚本不调用 Agent，也不修改阶段为“竞标完成”。`script_preparation_only` 说明脚本自身只做准备；`next_action` 是交给 Leader 的调度提示，不证明任何阅读或审查已经发生。通过终端独立运行脚本不会凭空启动 Agent。
- H1 必须真实展示候选并等待选择，提供题目/数据地址不等于模型批准；最终人工接管也不变。

## 必须收集题目与数据地址

没有来源时，Agent 主动问：

> 请提供题目文件和全部数据附件的本地路径，或上传文件/提供可访问下载链接；如果没有独立数据附件，请说明。

不要要求用户重新 clone、搬运框架脚本或填写 `AGENTS.md`。Bootstrap 准备的是本题实际输入和运行工作区，不是再安装一份仓库。

- 已有题目但缺数据：只追问数据附件地址；已有数据但缺题面：只追问题目地址。用户已明确提供的内容不重复询问。
- 用户明确“没有独立附件/数据在题面内”时核对题面说明，不强制凑一个数据文件；不能仅凭文件扩展名、文件数量或名称推断题面和数据齐全。
- 在冻结前，利用用户说明及题面中的附件清单确认文件对应关系、文件可读取；发现附件缺失、子目录被跳过或压缩包未检查时继续收集。压缩材料需要通过安全工具检查/解压到不覆盖原件的位置，不能把“已登记一个 ZIP”当作里面的内容已经可用。此处只核对输入，不提前发表题意或模型倾向。
- 材料齐备后，移除 `--prepare-only`，用重复的 `--source` 固定本次核对的文件集合，再运行 Bootstrap 完成冻结和自检。若手动低层初始化已冻结了不完整材料，应保留旧 run，明确说明原因并使用新的未占用运行目录，不覆盖旧 manifest。
- 完成回复应包含：题目路径、数据附件路径（或无独立附件的依据）、run 路径、缺项/依赖状态。只有目录存在、脚本运行成功或 `source_completeness_checked=false` 的报告，都不足以宣布本题准备完成；该字段表示脚本不做语义判断，由 Agent 核对，不能为了通过检查改成 true。

## 默认路径与重复执行

- 原始材料：仓库内 `raw-sources/`，不复制、不修改原件。
- 新 run：仓库内 `run/`。这两个目录已被 `.gitignore` 排除。
- 多题或需要重新冻结材料：显式指定新的目录，例如 `--run-dir runs/case-02`；不覆盖旧 run。
- 既有 run 已登记外部来源、且默认材料目录为空时，重复 init 会核对原登记来源，无需再输入参数。
- `init_run.py` 保留为低层工具；普通用户从 Bootstrap 进入，不直接手工编辑 manifest 或状态文件。

这是 Agent 指令路由，不是 shell hook 或平台的 `/init` 命令。Agent 必须加载本仓库 `AGENTS.md`；不自动加载的客户端，明确让它先读该文件。Codex 的发现机制见 [官方 AGENTS.md 文档](https://learn.chatgpt.com/docs/agent-configuration/agents-md)。
