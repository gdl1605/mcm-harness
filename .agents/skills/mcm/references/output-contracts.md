# Output Contracts

Serves `SKILL.md` Steps 0-9: define what each key stage must output so the workflow does not stop at vague prose.

Call this file when:

- 你知道该做什么，但还不够清楚“做完后该交什么”。
- 你需要把阶段产出压成表格、清单、固定标题块或决策卡片。
- 你怀疑当前回答只有思路，没有交付物。

## Hard Rule

- 面向比赛交付的内容默认中文。
- 启动、单问推进、中期复盘、最终审查、救火纠偏这些阶段，必须结构化输出。
- 如果这些阶段只给自由文字，视为未完成。

## 1. 关键阶段的最小输出格式

### 启动阶段

必须输出：

1. 全题拆题表
2. 依赖链说明
3. 高风险结构清单
4. 全题路线图
5. 前 4 小时最低交付物

最小字段：

| 问题 | 目标 | 题型 | 依赖 | 最终输出对象 | 高风险结构 |
| --- | --- | --- | --- | --- | --- |

### 单问推进阶段

必须输出：

1. 问题定义卡
2. 路线决策卡
3. 预处理与变量设计卡
4. 执行计划
5. 第一版 final answer artifact 草案
6. schema 对齐说明（若题目有 workbook、附件矩阵或固定结果表）

最小字段：

| 项目 | 最少内容 |
| --- | --- |
| 问题定义 | 目标、题型、依赖、最终输出对象 |
| 路线决策 | 主方案、是否需要备选、风险、验证 |
| 预处理 | 粒度、主键、时间轴、特征、proxy 说明 |
| 执行 | 输入、脚本、baseline、首个中间表、首个结果表 |
| 收口 | final answer artifact 草案、结论句 |
| schema 对齐 | 引用的 schema 文件，或至少锁定的表头、主键、时间粒度 |

### 中期复盘阶段

必须输出：

1. 当前阶段判断
2. 已完成/未完成交付物表
3. 每问收口状态
4. 最危险未收口问题
5. 接下来 2-3 小时推进清单

最小字段：

| 问题 | 当前状态 | 已有交付物 | 缺口 | 下一步 | 风险级别 |
| --- | --- | --- | --- | --- | --- |

### 写作冲刺阶段

本文件不规定论文段落或摘要字段。先按 `references/paper-writing.md` 选择 `paper-material`、`submission-draft` 或 `judge-review`，再由题目的答案形状决定材料结构。

写作阶段仍需保留可核验关系：每问的主答案能回到结果表、图或检验；正文引用的数值能回到 schema 或最终结果文件；附录与正文口径一致。除此之外，不以固定表格、句式或章节槽位判断论文完成度。

### 最终审查阶段

必须输出：

1. P0/P1/P2 缺口清单
2. 修补动作
3. 必删内容
4. 最后补漏顺序

最小字段：

| 优先级 | 缺口 | 为什么影响交卷 | 修补动作 | 是否已完成 |
| --- | --- | --- | --- | --- |

### 救火纠偏阶段

必须输出：

1. 当前失败点
2. 删什么
3. 保什么
4. 先补什么
5. 简化路线
6. 接下来 2/4/6 小时动作清单

最小字段：

| 项目 | 内容 |
| --- | --- |
| 失败点 |  |
| 立即停止 |  |
| 保留资产 |  |
| 优先补的交付物 |  |
| 简化路线 |  |
| 时间盒动作 |  |

## 2. 关键对象的最小模板

### 逐问题型判断模板

| 问题 | 目标 | primary type | secondary type | upstream/downstream | 数据粒度 | 高风险结构 |
| --- | --- | --- | --- | --- | --- | --- |

### 主方案 / 备选方案模板

| 问题 | 风险等级 | 主方案 | 是否需要备选 | 备选方案或“不需要备选”的理由 | 主要风险 | 验证方式 | 最终输出对象 |
| --- | --- | --- | --- | --- | --- | --- | --- |

### 单问完整通路模板

```text
[问题编号]
1. 问题定义：
2. 主方案：
3. 是否需要备选，为什么：
4. 预处理与变量：
5. 执行步：
   - 输入：
   - baseline：
   - 中间表：
   - 第一版结果表/图：
6. 验证：
7. final answer artifact：
8. 结论句：
```

### 验证计划模板

| 待验证结果 | baseline | 关键扰动参数/替代对照 | 可行性检查 | 合理性解释 | 当前缺口 |
| --- | --- | --- | --- | --- | --- |

### final answer artifact 模板

至少包含：

| 对象 | 结论/推荐/判定 | 依据 | 风险或备注 |
| --- | --- | --- | --- |

如果没有“对象”和“结论”两列，这个 artifact 视为未完成。

若题目有 workbook、附件矩阵或固定结果表，final answer artifact 必须先对齐对应 schema 的最终答卷支撑列；若仓库中已有匹配的年份/题型 schema，优先引用该文件。

### schema / template 落点规则

- 题目给 workbook、附件矩阵、命名结果文件或固定粒度表时，单问推进阶段必须写明对应的 schema 文件；没有专属 schema 时，至少注明已按 `assets/xlsx-schema/generic_answer_sheet_rules.md` 锁定表头。
- 写作冲刺阶段必须说明“正文引用的是 schema 中哪些最终答卷支撑列”，不能只围绕中间分析列写结论。
- 第一版 final answer artifact 导出后，应先运行 `scripts/deliverable_lint.py`；摘要使用 `assets/paper-template/abstract-stress-test.md` 做读者复述与反向核验，不做字段填空检查。


### 数据初检记录模板

当已经读入真实数据时，建模前至少补一张初检表；若缺失，视为尚未完成预处理入口检查。

| 检查项 | 发现 | 风险级别 | 回退动作 | 是否影响 schema/模型 |
| --- | --- | --- | --- | --- |
| 主键/粒度 |  | 高/中/低 |  |  |
| 时间轴 |  | 高/中/低 |  |  |
| 缺失/重复/单位 |  | 高/中/低 |  |  |
| proxy target |  | 高/中/低 |  |  |
| 重复测量/阈值时点/分阶段筛选 |  | 高/中/低 |  |  |

如果发现阈值达标时间或高维判别结构，必须说明是否触发 `assets/snippets/conditional/time_threshold_crossing.py` 或 `assets/snippets/conditional/pls_da.py`；不触发也要写理由。

### 风险清单模板

| 风险点 | 影响环节 | 当前状态 | 修补动作 |
| --- | --- | --- | --- |

## 3. 哪些情形必须结构化输出

这些地方必须用表格、编号清单或固定标题块：

- 全题拆题
- 逐问题型判断
- 主方案/备选方案决策
- 预处理设计
- 数据初检记录
- 执行计划
- 执行进度记录
- 验证计划
- final answer artifact
- 中期复盘
- 最终审查
- 救火纠偏

这些地方可以在结构化输出之后补自然段：

- 模型原理解释
- 图表现象解读
- 结果分析段
- 结论段

如果以下场景只给自由文字，视为未完成：

- 启动阶段
- 单问推进阶段
- 中期复盘阶段
- 最终审查阶段
- 救火纠偏阶段

## 4. 语言规则

- 比赛论文、答卷、表格标题、图注、阶段汇报、结论段默认中文。
- 代码注释可中英混合，但正文说明优先中文。
- 模板示例默认用中文字段名。
- 除非用户明确要求英文，否则不要输出英文版段落或英文审查意见。
