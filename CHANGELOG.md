# Changelog

## 0.3.0 — 2026-09-04

- 补齐 L2C/H1 对人展示责任：聊天直接说明逐问全部实际候选去向、重点竞品优劣、对应论文及证据如何影响取舍，不能用首选摘要或文件链接替代。
- 新增选型展示正文模板与不可覆盖的 H1 展示/依据版本快照约定；合并候选保留原名称，缺文献和历史展示记录如实标记，重新 H1 同样适用。
- 新增 `model-briefing` 发送前机械检查及回归案例；不预先要求人工批准，不判断展示语义或真实送达。旧 `route`/`data` 对展示产物缺失仅提示，不追溯阻断既有 run。

- 将 Bootstrap 使用方式放到 README 最前面：clone 后在 Agent 中说“初始化 / init”。
- 明确主动询问题目和数据附件地址，收到回复后继续初始化；支持由 Agent 处理用户指定目录、上传文件与可访问下载链接。
- 增加 `--prepare-only` 来源预检；先核对题面与附件齐备，再建立新 run，避免过早冻结部分材料。
- 材料核对与准备完成后，普通 init 默认由 Leader 自动进入材料阅读和模型路线竞标，到 H1 等待真实选择，无需用户再发启动 prompt；新增 `--setup-only` 保留明确只做准备的意图，已有 run 不重跑或越过人工 gate。

验证：全量 57 项测试通过，包含 6 项选型展示机械/协议接线测试；另附 7 个合成语义反例供审读，不将其当作真实任务的 Agent 行为验证。模型选择偏保守、绘图效果及高 token/时间开销等已知限制仍见 README。

## 0.2.1-alpha — 2026-09-04

- 新增默认 Bootstrap：用户在 Agent 中要求“初始化 / init”时，自动准备材料入口、创建有来源的运行目录并做基础检查。
- 重复初始化只核对已有来源、Skill 快照和阶段，不覆盖历史；缺材料时不冻结空来源 run。
- 外部技能只做本地哈希与缺项提示，平台能力交由 Agent 核对；不自动安装、不自动进入建模。
- 简化 README 快速开始，补齐 Agent 路由、Bootstrap 使用说明与隔离回归测试。

验证：43 项测试通过，覆盖无材料初始化、重复执行、来源变更、Skill 漂移、已有进度保护及原有流程工具。此验证不等于完整建模或绘图质量认证；外部能力仍须按阶段确认。

## 0.2.0-alpha — 2026-09-03

### Added

- 根目录 MIT License，版权人为 gdl1605。
- 版权与第三方组件说明、贡献指南、安全政策及发布前检查清单。
- README 的快速开始、运行依赖、许可与使用边界说明。
- 方法语义自包含、竞赛贡献证据两份内置 Skill 参考，以及对应的前向测试案例。

### Changed

- 扩充忽略规则，排除本地运行数据、原始资料、凭据、缓存和个人工具配置，同时保留内置 mcm Skill 与示例资源。
- V6 对多问题任务先独立重建证据允许的答案，再对照作者意图与题间接口。
- CP3A 在不读取代码和章节草稿的前提下重建方法，再进入材料对照审查；贡献以验证后的答案增量为依据。
- PW5A/PW5B 将首页独立重建与全文对照分为两遍，同步更新角色、配置、模板和上下文隔离规则。
- README 明确 C 题全流程自动化用途、H1 模型选择及重新触发条件、FD7 人工接管边界。

### Known limitations

- 模型选择仍可能偏保守，绘图效果仍需人工复核和调整。
- 维护者提供的全流程经验开销约为 8 亿 token、20 小时，不是固定开销或标准化性能基准。
- 正式绘图依赖外部技能与指定模型；未分发的依赖及许可边界见 `NOTICE.md`。

这是预发布版本。现有 23 项工具测试通过，不代表已完成本版本的完整竞赛回归、语义质量或视觉质量认证。
