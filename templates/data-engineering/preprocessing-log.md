# 数据预处理日志：{{TITLE}}

> 本日志记录实际执行了什么、影响多少和偏离了什么，不反向美化计划。以下项目是最低责任，不限制实现者记录新的步骤、失败尝试或异常。

## 运行元数据

- Run / source snapshot：{{RUN_AND_SNAPSHOT}}
- 数据版本：{{DATA_VERSION}}
- 代码版本/入口：{{CODE_VERSION_AND_ENTRYPOINT}}
- 运行时间与环境：{{TIME_AND_ENVIRONMENT}}
- 输入文件与哈希：{{INPUT_HASHES}}
- 显式参数：{{PARAMETERS}}

## 步骤日志

逐步记录目的、依据、输入/输出路径、实际操作、处理前后行数/对象数/时间范围、删除/新增/变更规模、警告和对应计划项。保留足以回溯到原始记录的映射。

## 连接、聚合与单位转换

记录连接键和覆盖率、未匹配记录、聚合规则、时间窗、单位换算公式及参数。说明它们影响哪些问题和接口。

## 缺失、零值、重复与异常处理

分别记录各类状态的识别依据和处置数量；不要把“未发生”“不可观测”“不适用”折叠成普通缺失而不留痕。

## 计划偏离与实现中决策

说明偏离原因、证据、替代方案、受影响产物和是否获得 Leader 决定。不得把临时代码行为静默升级为数据契约。

## 产物索引与质量事实

列出生成路径、版本、表数、行列数、主键/时间/单位声明、哈希或其他机械事实。机械全绿不等于语义正确。

## 失败运行、旧版本与回滚

记录失败尝试、被替换产物、恢复点和仍需重跑的下游；不删除首版和失败证据。

## 实现中出现的新发现

记录任何会改变题意、路线、数据契约、总体或接口的异常，并说明已停止、保留分支还是请求重开。

## 尚未执行

列出未做的模型特定特征、训练、求解、正式验证、论文图表和写作。

