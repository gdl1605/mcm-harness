# 数据工程交接：{{TITLE}}

> 这是当前可用数据版本的开放工作交接，不是“数据绝对正确”证明。以下问题是最低责任，不是字段白名单；后续发现新的限制或接口时必须补充并按版本重开。

## 当前版本与生成入口

- Run / source snapshot：{{RUN_AND_SNAPSHOT}}
- 当前数据版本：{{DATA_VERSION}}
- 代码版本与从零生成命令：{{PIPELINE_ENTRYPOINT}}
- 依赖与显式参数：{{ENV_AND_PARAMETERS}}
- 被替换但保留的版本：{{RETAINED_VERSIONS}}

## 可使用的数据产品

列出 staging、canonical 和各问 analytical 视图的路径、用途、对象、主键、粒度、时间、单位和覆盖范围。说明哪些是共享数据，哪些只服务特定问题。

## 字段与语义契约

说明核心 observed、derived、latent、proxy、label 和 decision quantity 的含义、来源与适用边界。链接完整数据契约，不复制成看似封闭的字段白名单。

## 关键处理与影响规模

索引预处理计划/日志，概括会影响总体、权重、接口或结论解释的处理，以及对应的行数、对象数和时间点变化。

## 允许、禁止与有条件使用

明确各问可使用的信息、预测/决策时点不可用的信息、禁止制造的标签、仍需保留 proxy 限制的字段和模型特定处理边界。

## 复现与接口审查结论

链接原始 reviewer 报告和实现者回应，区分已修复工程问题、仍保留语义异议、未执行检查和不能由机械检查证明的内容。

## 下游必须遵守的接口

逐问说明输入对象、字段、主键、粒度、单位、时间窗、可用时点、版本和不得静默改变的口径。

## 未决争议、分支与已知限制

保留少数意见、不可回答边界、竞争数据版本和适用条件；不要用综合摘要抹平 reviewer 异议。

## 变更请求、切换与局部回滚

说明模型模块怎样提出新数据需求，什么观察触发新 analytical 视图、重开数据契约、回到路线/题意，以及最早受影响版本和需要重跑的下游。

## 产物与证据索引

链接原始来源、契约、剖析、风险审查、计划、代码、日志、处理后数据、工程文稿、复现/接口复核和集中回应。

## 任务框架之外的新发现

记录任何尚未归类、但会改变下游建模、验证、答卷或论文边界的内容。

## 停止边界

本交接形成后数据模块停止主动扩张。尚未执行：模型训练、超参数选择、优化求解、正式模型验证、论文级绘图、答卷生成和正式论文写作。

