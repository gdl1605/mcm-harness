# 数据剖析报告：{{TITLE}}

> 本文报告附件实际是什么，不按预想路线或模型美化数据。以下问题是最低责任，不是字段白名单；可自由增加异常结构、图表或证据附录。本阶段允许结构与质量 EDA，不做模型训练或求解。

## 范围、版本与可复查入口

- Source snapshot：{{SOURCE_SNAPSHOT_ID}}
- 读取的文件/sheet/表：{{INPUT_SCOPE}}
- 使用的只读检查或脚本：{{PROBE_ENTRYPOINTS}}
- 未读取或无法读取的材料：{{UNREADABLE_OR_EXCLUDED}}

## 文件、表与字段实况

说明文件格式、sheet/表关系、字段名、类型、编码、记录规模及与数据说明的差异。保留原始定位信息。

## 对象、粒度、主键与连接

提出主键候选并报告唯一性、重复模式、连接覆盖和无法匹配的记录。区分合理重复、重复测量和疑似脏重复。

## 时间、单位与覆盖范围

记录时间起止、采样频率、可用时点、空间/人群覆盖、量纲和数量级。指出同名字段或不同表之间的口径冲突。

## 缺失、零值、异常与编码

分别描述 missing、0、未发生、不可观测、不适用和结构性空白；报告异常值、截断、哨兵值与不一致编码，但不替 Leader 决定如何处理。

## 数据说明与附件冲突

列出可定位证据、可能解释、受影响问题及最低成本复核方式。

## 未被路线使用的材料

检查未使用的表、字段、模板位置或记录类型是否会改变题意、总体、接口或风险判断。

## 当前能回答与不能回答什么

基于数据事实说明可构造量、不可构造量、只能作为 proxy 的量和仍需确认的语义。不要把可计算性等同于题意正确性。

## 框架外异常与新发现

完整记录既有问题没有预见的结构、关系或失败机制。

## 建议给 Leader 的证据与后续检查

列出最能改变数据方案的观察，以及本报告尚未执行的清洗、建模、求解和正式验证。

