# 验证上游变更请求：{{REQUEST_ID}}

> Validator 不直接修改上游；Leader 用本文把证据、影响和最早回滚点交回原 owner。以下是最低责任，可增加其他失效传播。

## 触发证据与原始版本

链接 review/probe/response/decision，记录受影响数据、合同、代码、run、结果和接口版本。

## 失效机制与竞争解释

说明当前已知事实、推断、反证、仍未确定内容和为什么不能在验证目录内修复。

## 最早回滚位置与原所有者

指定建模交接、M3/M4、M2、D0–D4、L2、L1 或更早阶段，以及应复用的原 owner。

## 最小修正范围与禁止项

说明允许改动的对象、必须冻结的口径、旧版本保留、不允许为绕过验证而做的修改。

## 失效传播与需重验主张

列出直接与下游失效产物、暂停使用的结果和修正后必须使用新独立证据复验的主张。

## 保留信息消耗与新验证要求

记录原 owner 已看到的验证信息和下一轮不能重复使用的 holdout。

## 新发现与不可回答边界

保留请求范围外但会改变修正范围或答卷表述的信息。
