# 独立验证 Task Brief：{{TASK_ID}}

> 本 brief 的问题是最低责任，不是输出字段白名单。角色必须继续报告任何会改变主张范围、题间接口或回滚位置的新发现。

## 阶段、角色与唯一目标

说明 V 阶段、验证单元、主张/问题范围和本轮唯一目标。

mcm profile 默认为 `auto`；若覆盖，记录 profile 与原因：{{MCM_PROFILE_OR_OVERRIDE_REASON}}

## 冻结版本与允许读取

逐路径列出题意、候选模型汇报、真实人工模型决定、路线、数据、build contract、代码、配置、run、结果、接口和已存验证产物的允许版本。

## 隐藏上下文与保留信息

列出不得读取的 peer 报告、Leader 倾向、builder 新辩护、论文材料和未授权保留信息；说明哪些信息已是开发反馈。

## 唯一主输出与工程写权

给出唯一主 Markdown 路径。默认没有额外写权；V3 需要时明确授权唯一 `validation/probes/PROBE-ID/`。

## A/B/C/D 最低问题

根据当前角色写出至少四个责任问题，要求连接事实、解释、影响、反例和区分动作。

## Probe 权限、暴露和预算

若是 V3，列出 probe ID、父 docket、允许打开的保留信息、暴露对象、运行/资源预算、冻结项和停止线。

## 停止与越权处理

说明什么时候必须停止，以及发现需修数据、模型、路线或题意时应如何报告而不直接修改。
