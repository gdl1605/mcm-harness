# 支撑材料：代码展示

> 以下是最低责任，不是字段白名单。ZIP 的 `source-code/` 必须包含完整原始脚本；本文档只负责论文末尾的可读展示，不能替代 ZIP 源文件。

代码量可控时展示完整代码；代码实在过多时，展示若干覆盖关键预处理、建模和结果生成步骤的原始片段。每段代码前说明脚本 ID、ZIP 内文件名、用途、版本/哈希、原始行号范围、执行顺序、输入、输出和支持的结果。使用正确语言标记的 fenced code block，保持所展示代码原貌，不用摘要或伪代码替代。列出因敏感信息、许可或缺失而未能收入的内容，不静默改写。
