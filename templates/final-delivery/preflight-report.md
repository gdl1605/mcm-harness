# FD3 机械预检报告

> 以下是最低责任，不是字段白名单。这里只记录机械可观察事实，不宣称排版美观、扣题或语义正确。

记录文件可读性、PDF 页数、逐页渲染、字体/公式/图片、表格溢出、空白页、乱码、交叉引用、匿名字段、附件和文件名检查。逐图记录 Figure ID、页码、`actual_embedded_width_mm`、manifest 批准/最小可读宽度、`actual_aspect_ratio`/批准长宽比、FR3 `in-paper-preview` 路径，并检查重叠、裁切、压缩、失真和目标宽度可读性；任一失败须明确写“FR3 关闭失效，返回 FR3”，不得冻结。必须记录“支撑材料”是否位于参考文献之后并实际展示结果和代码；记录 `supporting-materials.zip` 能否解包、是否无路径穿越、根目录清单，以及 `processed-data/`、`results/`、`source-code/` 是否各有非空文件。说明已在冻结前修复的纯机械问题，以及留给终审和人的问题。
