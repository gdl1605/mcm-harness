# 正式图渲染记录：{{FIGURE_ID}}

> 以下是最低责任，不是字段白名单。Pilot 不是正式证据，v1/final 必须使用完整冻结数据。

分别记录 v1、v2、final 的 render.py/环境/库版本、完整 skill chain、Python 后端、Cassatt2 profile/hash、`metbrewer_cassatt2` palette API/角色索引、配置、输入哈希、完整渲染命令、PNG/PDF/SVG 路径和哈希、实际物理宽高/长宽比、目标嵌入宽度、字体、数据行数、轴域、变换、警告、异常和未决问题。说明 2×2/其他布局的证据依据，以及总标题/长 Note 的外置路径；pilot 不能替代完整数据视觉检查。
