# Chart Contract：{{FIGURE_ID}}

> 以下是最低责任，不是字段白名单。v1 前冻结科学含义，不冻结视觉解法；看过完整数据首稿后才能收敛图型和布局。

先说明分析问题、支持的 claim、数据对象/粒度/单位/时间/样本量/误差、预期行数与信息密度、读者比较任务、必须保留的零点/对数/截断/限制、可能误导点和禁止变换。固定 `cassatt2_quiet_journal_v1`、`get_palette(..., 'metbrewer_cassatt2')`、稳定语义角色和非颜色冗余编码，但图型、画布比例、面板大小和正文位置在 v1 前只作为候选；2×2 仅在四个同类面板形成连贯比较且目标宽度可读时优先。v1 后记录选型、替代方案、视觉依据、目标嵌入宽度和切换条件。
