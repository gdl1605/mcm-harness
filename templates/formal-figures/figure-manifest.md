# 正式图 Manifest

> 以下是最低责任，不是字段白名单。只有本 manifest 授权的 final 图片可进入 FD0。

逐项记录 Figure ID、问题/章节、claim、final PNG/PDF/SVG、render.py、数据包、输入/输出哈希、v1/v2/final、两轮 iteration log、skill chain、Cassatt2 profile/hash、palette API/角色映射、布局类型与 2×2 适用依据、物理宽高/长宽比、FR3 通过宽度、最小可读宽度、review/response/closure、caption 状态、限制和禁止替换。列出撤销、附录和未决项；FD 改变宽度、比例、palette 或图例/caption 结构时必须使 FR3 关闭失效。
