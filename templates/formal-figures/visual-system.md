# 全篇正式图视觉系统

> 以下是最低责任，不是字段白名单。必须在全部完整数据 v1 实际可见后提炼，不得在首稿前写死视觉解法。

记录 `cassatt2_quiet_journal_v1` profile/hash、`$ssci-plots` palette API、主/参考/多分类角色映射、字体与中文回退、字号层级、白底/L 形轴/网格例外、非颜色区分、视觉层级、留白、图外总标题与长 caption、短面板标题、图例经济、数值格式、误差/参考线、地图/热图色图、PNG/PDF/SVG 导出和允许例外。记录哪些 v1 模式保留、哪些因默认感、不好看、Cassatt2 漂移、重复图例/caption、重叠、裁切、压缩或正文不可读而统一修正；不得复制/改写 palette HEX，也不得把 2×2 强加给不同信息密度的图。
