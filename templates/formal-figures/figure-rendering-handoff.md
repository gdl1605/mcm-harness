# 正式论文图交接

> 以下是最低责任，不是字段白名单。本交接不证明图的语义绝对正确，也不授权 FD0 从散乱目录挑图。

链接两个 skill lock/hash、Cassatt2 profile/hash、`$visualize-data → $ssci-plots → $nature-figure`/Python 调用、frozen inputs、v1 后 visual system、coverage map、manifest、两轮 iteration log、placement/caption handoff、figure review/response/closure、contact sheet、真实 in-paper preview 和未决 change request。说明所有新 subagent 的显式 `gpt-5.6-sol + high + fork_turns=none` 调度记录、最终图版本、通过宽度/最小可读宽度，以及 FD0 不得改变的数据、尺度、长宽比、Cassatt2 角色映射、图外 caption 结构和仍需人决定的审美偏好。
