# 正式论文图交接

> 以下是最低责任，不是字段白名单。本交接不证明图的语义绝对正确，也不授权 FD0 从散乱目录挑图。

链接 frozen inputs、visual system、coverage map、manifest、placement/caption handoff、figure review/response/closure 和未决 change request。说明所有新 subagent 的显式 `gpt-5.6-sol + high + fork_turns=none` 调度记录、最终图版本、FD0 不得改变的数据/尺度/颜色语义和仍需人决定的审美偏好。
