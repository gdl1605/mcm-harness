# 人工模型选择变更请求：{{REQUEST_ID}}

> 本请求只报告为什么需要重新决定，不自动修改人工选择。以下是最低责任，不是固定 schema。

## 当前人工决定与受影响问题

链接 `routes/human-model-decision.md`、当前 route/build contract、数据和运行版本。

## 新证据与具体失败机制

说明数据不可支撑、不收敛、目标/接口冲突或其他会迫使改变模型家族的证据；不要只写“效果不好”。

## 已尝试的合同内修正

...

## 需要人重新选择的候选与影响

列出可选替代、优劣势、文献状态、需要重跑的下游和时间/依赖代价。不得静默执行。

## 当前停止状态

`AWAITING_HUMAN_MODEL_DECISION`。收到真实回复前停止改变核心模型。

## 任务之外的新发现

...
