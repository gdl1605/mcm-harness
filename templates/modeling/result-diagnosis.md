# 构建结果独立诊断：{{QUESTION_AND_RUN}}

> 第一轮诊断只读冻结合同、run intent、代码和原始运行产物，不读实现者事后归因。以下问题是最低责任，不是故障枚举白名单；诊断者只读、不修代码。

## 独立性与可见材料

列出实际读取的文件、明确隐藏的 builder/Leader 解释、当前诊断版本和无法获得的证据。

## 冻结目标、意图与触发事实

准确复述问题、build contract、baseline、run intent 和 Leader 中性触发说明；区分观察事实与推断。

## 七类竞争解释

分别评估：实现缺陷；数值/求解/资源；算法细节；模型结构/假设；数据接口/可用时点/目标构造；评价口径；结果合理但期待无依据。

## 异常优秀专项检查

若结果异常地好，检查泄漏、重复、错误切分、未来信息、指标实现、目标穿越和缓存/隐式状态。

## 证据、反例与失效传播

对高影响解释给出代码/日志/结果/数据证据、可反驳观察、影响的答卷/接口/下游运行和不能确定的边界。

## 最小判别动作

提出最便宜、有区分力的下一动作、建议 L0/L1/L2/L3、冻结项、预算和回滚点。不要只说换模型或继续调参。

## 暂不可判与任务之外的新发现

保留需要双分支、后续验证、上游重开或用户判断的问题。

## 停止边界

本报告不裁决最终模型，不修改实现，不执行正式验证。
