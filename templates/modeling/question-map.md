# 建模问题依赖图：{{TITLE}}

> Leader 在 M0 形成当前执行图。以下内容是最低责任，不是假定题目固定有三问的 schema；可按真实问题结构增加共享构建单元、反馈环或竞争接口。

## 采用的上游版本

- 题意基线：{{PROBLEM_BASELINE}}
- 候选模型人工汇报：{{MODEL_CANDIDATE_BRIEFING}}
- 真实人工模型决定：{{HUMAN_MODEL_DECISION}}
- 路线交接：{{ROUTE_HANDOFF}}
- 数据交接与版本：{{DATA_HANDOFF_AND_VERSION}}
- 相关原始 memo / 少数意见：{{UPSTREAM_MEMOS}}

## 每问答案对象与数据入口

逐问说明要回答的对象、输入分析视图、总体、主键、粒度、单位、时间、可用时点和输出对象。不可构造或仅能 proxy 的内容保持显式。

## 人工授权的模型边界

逐问列出获准主模型、baseline、保留备选/挑战/敏感性模型和明确放弃项。新模型只能作为 change request，不能在 M0/M1 自行替换。

## 依赖、共享构建单元与反馈

说明父问题、消费问题、共享状态/参数/代码、反馈关系和可以并行的独立单元。不要把文件顺序当成真实依赖。

## 题间接口

逐个接口写生产者、消费者、对象、主键、粒度、单位、时间、版本、不确定性和当前竞争口径。

## 开发反馈与保留信息

逐问列出构建时允许查看的信息，以及留给后续独立验证、不得用于反复调整的信息。若本题不存在可合理保留的信息，如实说明，不虚构 holdout。

## 代码与写入所有权

指定 shared-kernel owner、每问 builder 写入根、共享只读依赖和 M5 接口集成 owner 的产生规则。

## 失效传播

说明上游数据、合同、代码、参数或结果版本变化时，哪些下游 run/接口/文稿失效，哪些不受影响。

## 当前并行顺序与预算

写出关键路径、可并行单元、优先级、并发槽使用和各问初始预算。

## 未决争议、重开条件与框架外发现

保留不能在 M0 判定的结构、接口和信息边界；说明最早回退位置。
