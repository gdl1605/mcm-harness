# 构建运行 Memo：{{RUN_ID}}

> 本文在运行后形成，不能修改 run intent。以下问题是最低责任，不限制增加日志、结果表或异常附录。

## 运行身份与可追溯入口

索引 run intent、build contract、data/code/config、命令、环境、随机种子、父运行和输出路径。

## 实际执行与状态

说明实际执行步骤、耗时、求解/收敛/可行状态、警告、失败、重试和与 intent 的偏离。

## 候选结果与接口

链接机器可读结果表和题间接口，说明对象、主键、粒度、单位、时间、版本和当前是否可被下游消费。

## 与 baseline 和预期画像的差异

报告执行状态、数学一致性、回答对齐、相对表现、形态/常识、稳定迹象、异常优秀和接口消费方面的观察。

## 实际使用的信息

说明用了哪些开发反馈，是否意外接触保留信息、隐藏状态、缓存或未声明数据。

## 原实现者的竞争解释

区分实现、数值/求解、算法细节、模型结构、数据接口、评价口径和正常但不理想；标记证据与未知。

## 建议动作与冻结项

提出建议等级、最小判别动作、预期反证、必须冻结的内容、预算、影响范围和回滚点；Leader 未授权前不实施非平凡修改。

## 失败、异常和任务之外的新发现

保留所有失败与异常观察，即使它不支持当前路线。
