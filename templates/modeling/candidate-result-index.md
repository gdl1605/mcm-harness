# 候选结果索引：{{QUESTION_OR_BUILD_UNIT}}

> 本文索引候选结果和来源运行，不宣布最终模型。以下是最低责任；可增加多目标、时空、仿真或优化特有字段说明。

## 当前合同、数据与结果口径

索引 build contract、数据版本、结果对象、主键、粒度、单位、时间和接口版本。

## Baseline

列出候选名、run、代码/config、机器结果路径、状态、开发观察、限制和被替换版本。

## 主候选

列出当前活跃版本、来源 run、结果路径、与 baseline 的同口径差异、异常和待验证主张。

## 挑战候选

列出实质结构差异、来源 run、结果路径、当前存活/休眠/拒绝状态和不能由开发反馈裁决的事项。

## 失败、不可行和被回滚运行

保留失败原因、诊断、调整和版本关系，不只索引最佳结果。

## 题间用途与失效传播

说明哪些结果被哪些下游消费，上游版本变化会使什么失效。

## 已用反馈、保留信息和不能支持的主张

区分候选生成时已使用的开发信息和未打开的验证信息；列出不能直接写入论文的结论。

## 开放发现

记录结果索引之外会改变验证或交接的新事项。
