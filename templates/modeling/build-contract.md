# 建模构建合同：{{QUESTION_OR_BUILD_UNIT}}

> Leader 综合 M1 原报告形成当前可执行版本。它冻结比较口径和修改权限，不证明模型正确。以下内容是最低责任，不是语义 JSON schema。

## 依据、版本与未被替代的原报告

索引上游交接、question map、M1 三份原报告、少数意见、当前合同版本和父合同。

## 数学对象

说明集合、变量、参数、目标、约束、假设、量纲、边界和最终输出；保留竞争解释。

## 数据与信息边界

冻结数据版本、视图、字段、主键、粒度、单位、时间、可用时点、开发反馈和不得打开的保留信息。

## baseline、主候选与挑战候选

链接真实人工模型决定，说明每个获准候选的责任、实质结构差异、统一输入/输出口径、当前活跃/休眠/拒绝状态及理由。挑战/敏感性候选必须在人工保留范围内；新模型只能作为 change request，不能因实现方便静默替换。

## 结果预期画像

预先说明合理结果可能具有的对象、范围、符号、数量级、形状、约束状态和接口；不要设通用“达标分数”。

## 代码、配置、结果与所有权

指定 builder、shared-kernel owner、写入路径、入口、配置、结果表、题间接口和旧版本保留方式。

## 调整包络

分别列出预授权 L0/L1、需要升版合同的 L2、必须退回上游的 L3。判断按语义变化，不按代码行数；改变模型家族、目标或核心结构时必须返回 H1 重新取得真实人工决定。

## 分支、时间与计算预算

说明主/挑战分支、额外分支条件、单轮主要假设、资源预算和停止搜索条件。

## 触发、失效传播与回滚

说明异常差、异常好、约束失败、接口失败等触发信号；合同或上游变化会使哪些 run/下游失效。

## 未决争议和任务之外的新发现

保留当前不能裁决、应交给诊断/验证/用户或上游的问题。
