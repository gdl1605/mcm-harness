# 数学规格：{{QUESTION_OR_BUILD_UNIT}}

> 本文由隔离的数学规格架构师形成，只定义“要计算什么”。以下是最低责任，不限制增加竞争规格、关系图或公式附录。

## 依据与范围

索引题意、路线、数据交接、question map、source snapshot 和本规格版本；说明未读取的 peer 报告与实现结果。

## 集合、索引与对象

定义总体、对象、集合、索引、空间/时间范围和分析单位。

## 符号、变量与参数

区分 observed、derived、latent、proxy、label、state、decision quantity、parameter 和 output；给出单位、来源、可用时点和未知语义。

## 数学关系

写出目标函数、约束、状态转移、观测方程、估计对象、初边值、边界条件和必要定义。每项标记题面直接依据、数据依据、推断或暂用假设。

## 数据映射与可构造性

逐项映射数据视图/字段，说明不可观测、只能 proxy、需派生、缺少或会引入未来信息的量。

## baseline、主结构与竞争结构

描述三者的数学差异和额外证据负担；无法构成实质竞争时如实说明。

## 识别、可行、量纲与边界条件

说明什么情况下模型无定义、不可识别、不可行、尺度错误或会产生多解。

## 题间输入输出

说明本问消费什么、生产什么，以及对象、主键、粒度、单位、时间、版本和不确定性。

## 上游重开条件与新发现

记录会推翻规格的字段、题意、路线或接口观察，以及任务之外的新结构。
